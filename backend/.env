# MongoDB Atlas Cloud Connection
MONGODB_URI=mongodb+srv://USERNAME:PASSWORT@CLUSTER.mongodb.net/akutengel?retryWrites=true&w=majority

# Server Port
PORT=3000

# Node Environment
NODE_ENV=development
```

**⚠️ WICHTIG: Ersetze:**
- `USERNAME` = dein MongoDB Atlas Username
- `PASSWORT` = dein MongoDB Atlas Passwort
- `CLUSTER` = dein Cluster-Name (z.B. `akutengel-cluster.abc123`)

---

### SCHRITT 2: MongoDB Atlas Account (falls noch nicht)

**Hast du MongoDB Atlas Account?**

#### **NEIN** → Schnell erstellen (10 Min):

1. **Gehe zu:** https://www.mongodb.com/cloud/atlas/register

2. **Registrieren:**
   - Email
   - Passwort
   - "Sign Up"

3. **Kostenloses Cluster:**
   - "Build a Database"
   - **"M0 FREE"** wählen (links, kostenlos!)
   - Provider: **AWS**
   - Region: **Frankfurt** (eu-central-1)
   - Cluster Name: `akutengel-cluster`
   - **"Create"** (warten 2-3 Min)

4. **Database User erstellen:**
   - Username: `akutengel-admin`
   - Password: **Generieren & kopieren!** 📋
   - "Create User"

5. **IP Whitelist:**
   - "Network Access"
   - "Add IP Address"
   - **"Allow Access from Anywhere"** (0.0.0.0/0)
   - "Confirm"

6. **Connection String kopieren:**
   - "Connect"
   - "Connect your application"
   - **Driver:** Node.js
   - **Copy String:**
```
     mongodb+srv://akutengel-admin:<password>@akutengel-cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
```
   
7. **String anpassen:**
   - Ersetze `<password>` mit echtem Passwort
   - Füge `/akutengel` VOR dem `?` ein:
```
     mongodb+srv://akutengel-admin:PASSWORT@cluster.xxxxx.mongodb.net/akutengel?retryWrites=true&w=majority